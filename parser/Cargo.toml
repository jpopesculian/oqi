[package]
name = "oqi-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
oqi-lexer = { path = "../lexer" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
